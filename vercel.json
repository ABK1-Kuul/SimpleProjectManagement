{
  "buildCommand": "vite build && esbuild src/vercelApi.ts --bundle --platform=node --format=esm --outfile=api/index.js",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
